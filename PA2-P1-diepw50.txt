/*

ltl ltl_0: [] ((! (((statusA==1)) && ((statusB==1)))) || (((partnerA==agentB)) && ((partnerB==agentA))))
starting claim 3
Never claim moves to line 4	[(1)]
  2:	proc  1 (Bob:1) needham-starter.txt:84 (state 3)	[partnerB = agentI]
  4:	proc  1 (Bob:1) needham-starter.txt:84 (state 4)	[pkey = keyI]
  6:	proc  0 (Alice:1) needham-starter.txt:44 (state 1)	[partnerA = agentB]
  8:	proc  0 (Alice:1) needham-starter.txt:44 (state 2)	[pkey = keyB]
 10:	proc  0 (Alice:1) needham-starter.txt:22 (state 7)	[message.key = pkey]
 10:	proc  0 (Alice:1) needham-starter.txt:23 (state 8)	[message.content1 = 6]
 10:	proc  0 (Alice:1) needham-starter.txt:24 (state 9)	[message.content2 = 3]
 12:	proc  0 (Alice:1) needham-starter.txt:50 (state 11)	[network!msg1,partnerA,message.key,message.content1,message.content2]
 13:	proc  1 (Bob:1) needham-starter.txt:88 (state 7)	[network?msg1,agentB,data.key,data.content1,data.content2]
 15:	proc  1 (Bob:1) needham-starter.txt:91 (state 8)	[((data.key==keyB))]
 15:	proc  1 (Bob:1) needham-starter.txt:92 (state 9)	[pnonce = data.content2]
 17:	proc  1 (Bob:1) needham-starter.txt:22 (state 10)	[message.key = pkey]
 17:	proc  1 (Bob:1) needham-starter.txt:23 (state 11)	[message.content1 = pnonce]
 17:	proc  1 (Bob:1) needham-starter.txt:24 (state 12)	[message.content2 = 2]
 19:	proc  1 (Bob:1) needham-starter.txt:96 (state 14)	[network!msg2,partnerB,message.key,message.content1,message.content2]
 20:	proc  2 (Intruder:1) needham-starter.txt:122 (state 1)	[network?tag,_,data.key,data.content1,data.content2]
 22:	proc  2 (Intruder:1) needham-starter.txt:125 (state 6)	[(1)]
 24:	proc  2 (Intruder:1) needham-starter.txt:128 (state 9)	[((data.key==keyI))]
 26:	proc  2 (Intruder:1) needham-starter.txt:129 (state 10)	[knows_nonceA = ((knows_nonceA||(data.content1==nonceA))||(data.content2==nonceA))]
 28:	proc  2 (Intruder:1) needham-starter.txt:132 (state 11)	[knows_nonceB = ((knows_nonceB||(data.content1==nonceB))||(data.content2==nonceB))]
 30:	proc  2 (Intruder:1) needham-starter.txt:140 (state 17)	[tag = msg2]
 32:	proc  2 (Intruder:1) needham-starter.txt:144 (state 21)	[recpt = agentA]
 34:	proc  2 (Intruder:1) needham-starter.txt:150 (state 29)	[data.key = keyA]
 36:	proc  2 (Intruder:1) needham-starter.txt:157 (state 36)	[(knows_nonceA)]
 38:	proc  2 (Intruder:1) needham-starter.txt:157 (state 37)	[data.content1 = nonceA]
 40:	proc  2 (Intruder:1) needham-starter.txt:163 (state 45)	[(knows_nonceB)]
 42:	proc  2 (Intruder:1) needham-starter.txt:163 (state 46)	[data.content2 = nonceB]
 44:	proc  2 (Intruder:1) needham-starter.txt:168 (state 52)	[network!tag,recpt,data.key,data.content1,data.content2]
 45:	proc  0 (Alice:1) needham-starter.txt:53 (state 12)	[network?msg2,agentA,data.key,data.content1,data.content2]
 47:	proc  0 (Alice:1) needham-starter.txt:57 (state 13)	[(((data.key==keyA)&&(data.content1==nonceA)))]
 47:	proc  0 (Alice:1) needham-starter.txt:60 (state 14)	[pnonce = data.content2]
 49:	proc  0 (Alice:1) needham-starter.txt:22 (state 15)	[message.key = pkey]
 49:	proc  0 (Alice:1) needham-starter.txt:23 (state 16)	[message.content1 = pnonce]
 49:	proc  0 (Alice:1) needham-starter.txt:24 (state 17)	[message.content2 = 0]
 51:	proc  0 (Alice:1) needham-starter.txt:64 (state 19)	[network!msg3,partnerA,message.key,message.content1,message.content2]
 52:	proc  1 (Bob:1) needham-starter.txt:98 (state 15)	[network?msg3,agentB,data.key,data.content1,data.content2]
 54:	proc  1 (Bob:1) needham-starter.txt:99 (state 16)	[(((data.key==keyB)&&(data.content1==nonceB)))]
 56:	proc  1 (Bob:1) needham-starter.txt:101 (state 17)	[statusB = 1]
 58:	proc  0 (Alice:1) needham-starter.txt:66 (state 20)	[statusA = 1]
spin: _spin_nvr.tmp:3, Error: assertion violated
spin: text of failed assertion: assert(!(!((!(((statusA==1)&&(statusB==1)))||((partnerA==agentB)&&(partnerB==agentA))))))
Never claim moves to line 3	[assert(!(!((!(((statusA==1)&&(statusB==1)))||((partnerA==agentB)&&(partnerB==agentA))))))]
spin: trail ends after 59 steps
#processes: 3
		partnerA = agentB
		partnerB = agentI
		statusA = 1
		statusB = 1
		knows_nonceA = 1
		knows_nonceB = 1
 59:	proc  2 (Intruder:1) needham-starter.txt:120 (state 53)
 59:	proc  1 (Bob:1) needham-starter.txt:102 (state 18) <valid end state>
 59:	proc  0 (Alice:1) needham-starter.txt:67 (state 21) <valid end state>
 59:	proc  - (ltl_0:1) _spin_nvr.tmp:2 (state 6)
3 processes created

The fix to this vulernable protocol is to send the sending agent's ID in the second message. This allows the agent who initiated the exchange to verify that the nonces sent truly belong to the sender/receiver pair.
I added an extra field to the Crypt structure and then tested the model with the two ltl properties that check for all states if A is finished and A's partner is B then I does not know the nonce of A, and vice versa.
*/



mtype = { msg1, msg2, msg3, 
          keyA, keyB, keyI,
          agentA, agentB, agentI, 
          nonceA, nonceB, nonceI };

typedef Crypt { mtype key, content1, content2
    /* My fix: add a third field to crypt to pass agent's ID in message 2 */
    /* , content3 */
 };

chan network = [0] of { mtype, /* msg# */
                        mtype, /* receiver */
                        Crypt };


inline build(m, k, c1, c2 /* , c3 */) {
  m.key = k;
  m.content1 = c1;
  m.content2 = c2 /* ;
  m.content3 = c3 */
}

mtype partnerA;
mtype partnerB;
bit statusA; /* 1 = success */
bit statusB; /* 1 = success */


/*------------------------------------------------------------------*
 * ALICE
 *------------------------------------------------------------------*/

active proctype Alice() {
  mtype pkey;      /* the partner's public key           */
  mtype pnonce;    /* nonce that we receive from partner */
  Crypt message;   /* our message to the partner         */
  Crypt data;      /* received message                   */

  if /* choose a partner for this run */
  :: partnerA = agentB; pkey = keyB;
  :: partnerA = agentI; pkey = keyI;
  fi;

  /* Prepare and send first message */  
  build(message, pkey, agentA, nonceA /* , 0 */);
  network ! msg1, partnerA, message;
  printf("Alice sent message to %e", partnerA);
  /* Wait for answer (using pattern matching) */
  network ? (msg2, agentA, data);
  
  /* Proceed only if the key field of the data matches keyA and the
     received nonce is the one that we have sent earlier */
  (data.key == keyA) && (data.content1 == nonceA) /* && (data.content3 == partnerA) */ ;
  printf("Alice decoded message from %e", partnerA);
  /* Obtain partner's nonce */
  pnonce = data.content2;

  /* Prepare and send the last message */  
  build(message, pkey, pnonce, 0 /* , 0 */);
  network ! msg3, partnerA, message;
  printf("Alice sending final message to %e", partnerA);

  statusA = 1; 
}


/*------------------------------------------------------------------*
 * BOB
 *------------------------------------------------------------------*/

active proctype Bob() {
  mtype pkey;      /* the other agent's public key                 */
  mtype pnonce;    /* nonce that we receive from the other agent   */
  Crypt message;   /* our encrypted message to the other party     */
  Crypt data;      /* received encrypted message                   */

  /* Complete this */
  /* nondeterministic partner selection */
  if
  :: partnerB = agentA; pkey = keyA;
  :: partnerB = agentI; pkey = keyI;
  fi;

  /* receive the first message from alice */
  network ? (msg1, agentB, data);

  /* if the public key matched b's public key */
  (data.key == keyB);
  pnonce = data.content2;
  printf("Bob decoded message from %e", partnerB);

  /* send alice's nonce back with Bob's nonce */
  build(message, pkey, pnonce, nonceB /* , agentB */);
  network ! msg2, partnerB, message;
  printf("Bob sent message  %e", partnerB);

  network ? (msg3, agentB, data);
  (data.key == keyB) && (data.content1 == nonceB);

  statusB = 1; 
}

/*------------------------------------------------------------------*
 * INTRUDER
 *------------------------------------------------------------------*/

bool knows_nonceA, knows_nonceB;

inline copy(from, to) {
  to.key = from.key;
  to.content1 = from.content1;
  to.content2 = from.content2;
  /* to.content3 = from.content3; */
}

active proctype Intruder() {
  mtype tag, recpt;
  Crypt data, intercepted;

      do
      
      :: network ? (tag, _, data) ->
         if /* perhaps store the message */
         :: copy(data, intercepted);
         :: skip;
         fi;
         if /* record newly learnt nonces */
         :: (data.key == keyI) ->
            knows_nonceA = knows_nonceA || 
                           (data.content1 == nonceA) || 
                           (data.content2 == nonceA);
            knows_nonceB = knows_nonceB || 
                           (data.content1 == nonceB) || 
                           (data.content2 == nonceB);
         :: else -> skip;
         fi;
      :: /* Replay or send a message */
         if /* choose message type */
         :: tag = msg1;
         :: tag = msg2;
         :: tag = msg3;
         fi;
         if /* choose recipient */
         :: recpt = agentA;
         :: recpt = agentB;
         fi;
         if /* replay intercepted message or assemble it */
         :: copy(intercepted, data);
         :: if
            :: data.key = keyA;
            :: data.key = keyB;
            fi;
            if
            ::                 data.content1 = agentA;
            ::                 data.content1 = agentB;
            ::                 data.content1 = agentI;
            :: knows_nonceA -> data.content1 = nonceA;
            :: knows_nonceB -> data.content1 = nonceB;
            ::                 data.content1 = nonceI;
            fi;
            if
            :: knows_nonceA -> data.content2 = nonceA;
            :: knows_nonceB -> data.content2 = nonceB;
            ::                 data.content2 = nonceI;
            fi; /*
            if
            :: data.content3 = agentA;
            :: data.content3 = agentB;
            :: data.content3 = agentI;
            :: data.content3 = 0;
            fi; */
         fi;

         network ! tag, recpt, data;
      od
}


/* Write your property here */
ltl { [] (statusA == 1 && statusB == 1 -> partnerA == agentB && partnerB == agentA) }
ltl { [] (statusA == 1 && partnerA == agentB -> !knows_nonceA) }
ltl { [] (statusB == 1 && partnerB == agentA -> !knows_nonceB) }